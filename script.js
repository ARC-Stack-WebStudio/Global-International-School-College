document.addEventListener('DOMContentLoaded',()=>{const n=document.querySelector('.navbar');window.addEventListener('scroll',()=>n.classList.toggle('scrolled',scrollY>35));const counters=document.querySelectorAll('[data-count]');const run=()=>counters.forEach(el=>{if(el.dataset.done)return;el.dataset.done=1;let target=+el.dataset.count,current=0,step=Math.ceil(target/45);let id=setInterval(()=>{current=Math.min(current+step,target);el.textContent=current+(el.dataset.suffix||'');if(current===target)clearInterval(id)},28)});new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){run();e.target.disconnect()}}),{threshold:.35}).observe(document.querySelector('.hero-stats'));document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();const b=f.querySelector('button');b.innerHTML='Thank you! We will be in touch.';b.disabled=true}));});
